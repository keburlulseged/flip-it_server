let User,stripe;_b30‍.x([["createConnectAccount",()=>createConnectAccount]]);_b30‍.w("../models/user",[["default",["User"],function(v){User=v}]]);_b30‍.w("stripe",[["default",["stripe"],function(v){stripe=v}]]);


Stripe(process.env.STRIPE_SECRET);

       const createConnectAccount = async (req, res) => {
  const user = await User.findById(req.user._id).exec();
  _b30‍.g.console.log("USER ++> ", user);

  const account = await stripe.accounts.create({
    type: "express",
  });

  console.log(`ACCOUNT ===> ${account}`);
};
