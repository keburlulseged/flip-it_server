let User;_895‍.x([["register",()=>register]]);_895‍.w("../modles/user",[["default",["User"],function(v){User=v}]]);

       const register = async (req, res) => {
  _895‍.g.console.log(req.body);
  const { name, email, password } = req.body;

  if (!name) return res.status(400).send("Name is required");
  if (!password || password.length < 6)
    return res
      .status(400)
      .send("Password is required and should be at least 6 characters");
  let userExist = await (await User.findOne({ email })).exec();
  if (userExist) return res.status(400).send(`Email is taken`);

  const user = new User(req.body);
  try {
    await user.save();
    console.log(`User created: ${user}`);
    return res.json({ ok: true });
  } catch (err) {
    return res.status(400).send("ERROR: Try again");
  }
};
